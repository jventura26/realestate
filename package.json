{
  "name": "real-estate-sites",
  "version": "2.0.3",
  "description": "Two static real estate sites from one Wix CSV",
  "scripts": {
    "build:vinculo": "node src/vinculo/build.js && node copy-assets.js",
    "build:zona": "node src/zona/build.js",
    "build": "npm run build:vinculo && npm run build:zona && node post-build.js",
    "dev:vinculo": "npm run build:vinculo && npx serve dist/vinculo -p 3001",
    "dev:zona": "npm run build:zona && npx serve dist/zona -p 3002"
  },
  "dependencies": {
    "csv-parse": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
